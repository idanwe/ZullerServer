.page-header
  %h1 Listing clubs

%table.table.table-striped.table-bordered
  %tr
    %th Name
    %th Address
    %th Min age
    %th Logo
    %th Phone number
    %th Music
    %th Opening Hours
    %th Menu
    %th Alcohol Prices
    %th Weight
    %th

  - @clubs.each do |club|
    %tr
      %td= club.name
      %td= club.address
      %td= club.min_age
      %td= club.logo
      %td= club.phone_number
      %td= club.music
      %td= club.opening_hours
      %td= club.menu
      %td= club.alcohol_prices
      %td= club.weight
      %td
        = link_to 'Show', club_path(club, format: :html), class: 'btn btn-mini'
        = link_to 'Edit', edit_club_path(club, format: :html), class: 'btn btn-mini'
        = link_to 'Destroy', club_path(club, format: :html), method: :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-mini btn-danger'

%br

= link_to new_club_path(format: :html), class: 'btn btn-success' do
  %i.icon-plus.icon-white
  new club
