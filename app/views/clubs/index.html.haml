.page-header
  %h1 Listing clubs

%table.table.table-striped.table-bordered
  %tr
    %th Name
    %th Logo
    %th Min age
    %th Address
    %th Date
    %th Weight
    %th Phone number
    %th Music
    %th
    %th
    %th

  - @clubs.each do |club|
    %tr
      %td= club.name
      %td= club.logo
      %td= club.min_age
      %td= club.address
      %td= club.date
      %td= club.weight
      %td= club.phone_number
      %td= club.music
      %td= link_to 'Show', club_path(club, format: :html), class: 'btn btn-mini'
      %td= link_to 'Edit', edit_club_path(club, format: :html), class: 'btn btn-mini'
      %td= link_to 'Destroy', club_path(club, format: :html), method: :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-mini btn-danger'

%br

= link_to new_club_path(format: :html), class: 'btn btn-success' do
  %i.icon-plus.icon-white
  new club
