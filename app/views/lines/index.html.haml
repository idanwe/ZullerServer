.page-header
  %h1 Line

%table.table.table-striped.table-bordered
  %tr
    %th Name
    %th Host
    %th Address
    %th Phone number
    %th Contact name
    %th Contact phone number
    %th Facebook url
    %th Min age
    %th Logo url
    %th Photos url
    %th Musics
    %th Date
    %th Opening Hours Start
    %th Opening Hours End
    %th Day Of Week
    %th Created At
    %th Weight
    %th

  - Attraction.all[0]
  - @lines.each do |line|
    %tr
      %td= line.name
      %td= line.host.name
      %td= line.address
      %td= line.phone_number
      %td= line.contact_name
      %td= line.contact_phone_number
      %td= line.facebook_url
      %td= line.min_age
      %td= line.logo_url
      %td= line.photos_url
      %td= line.musics.join(', ')
      %td= line.date
      %td= line.opening_hours_start
      %td= line.opening_hours_end
      %td= Date::DAYNAMES[line.day_of_week] if line.day_of_week.present?
      %td= line.created_at
      %td= line.weight
      %td
        = link_to 'Show', line_path(line, format: :html), class: 'btn btn-mini btn-in-table'
        = link_to 'Edit', edit_line_path(line, format: :html), class: 'btn btn-mini btn-in-table'
        = link_to 'Delete', line_path(line, format: :html), method: :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-mini btn-danger btn-in-table'

%br

= link_to new_line_path(format: :html), class: 'btn btn-success' do
  %i.icon-plus.icon-white
  new line
