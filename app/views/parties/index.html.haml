.page-header
  %h1 Party

%table.table.table-striped.table-bordered
  %tr
    %th Name
    %th Host
    %th Address
    %th Phone number
    %th Contact name
    %th Contact phone number
    %th Facebook url
    %th Min age
    %th Logo url
    %th Photos url
    %th Musics
    %th Date
    %th Opening Hours Start
    %th Opening Hours End
    %th Day of Week
    %th Created At
    %th Weight
    %th

  - Attraction.all[0]
  - @parties.each do |party|
    %tr
      %td= party.name
      %td= party.host.name
      %td= party.address
      %td= party.phone_number
      %td= party.contact_name
      %td= party.contact_phone_number
      %td= party.facebook_url
      %td= party.min_age
      %td= party.logo_url
      %td= party.photos_url
      %td= party.musics.join(', ')
      %td= party.date
      %td= party.opening_hours_start
      %td= party.opening_hours_end
      %td= Date::DAYNAMES[party.day_of_week] if party.day_of_week.present?
      %td= party.created_at
      %td= party.weight
      %td
        = link_to 'Show', party_path(party, format: :html), class: 'btn btn-mini btn-in-table'
        = link_to 'Edit', edit_party_path(party, format: :html), class: 'btn btn-mini btn-in-table'
        = link_to 'Delete', party_path(party, format: :html), method: :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-mini btn-danger btn-in-table'

%br

= link_to new_party_path(format: :html), class: 'btn btn-success' do
  %i.icon-plus.icon-white
  new party
